# SDD ledger — plan: docs/plans/2026-09-08-ers-review-matter-architecture-migration.md

## Identity

- Issue: #185 / MIG-15
- BASE_SHA: `18aeab81fbfab52872a1cf63b7c8770241188bee`
- BRANCH: `feat/mig-15-matter-visual-integration`
- USER_DIRTY_CHANGES_PRESERVED = YES

## Preflight scan

| Task/interface row | Files or interface relationship | Finding | Ruling/status |
|---|---|---|---|
| MIG-15 visual binding → existing case visual intake | `case_visual.py`, `ImmutableAttachment`, `case_id` | New Matter linkage must consume existing case/attachment identity without renaming `case_id`. | Ruling: keep `case_id` as drawing artifact identity and add explicit Matter linkage around it; creating a second case identity would break existing consumers. Proceed. |
| MIG-15 visual binding → visual handoff | `visual_binding.py`, `visual_handoff.py`, `ReviewScope` | The handoff currently normalizes `QuestionPlan`/`ReviewScope`; new Matter issue context must not bypass scope validation. | Ruling: normalize validated `ReviewScope` through the existing adapter and retain legacy `QuestionPlan` compatibility; do not make visual AI output authoritative. Proceed. |
| MIG-15 visual binding → visual pages/decoder | `visual_pages.py`, #165/#166 MIME/decoder boundary | Page caches and decoder checks are existing authority boundaries; new binding must validate exact attachment/source identity before reuse. | Proceed with exact attachment ID/source hash checks and preserve current decoder/page contracts. |
| MIG-15 own file naming | Issue text says `drawing_binding.py`/two tests; migration plan says `visual_binding.py`/one test. | Conflicting descriptive filenames. | Ruling: use the migration plan's canonical `visual_binding.py` and `test_visual_binding.py`; do not duplicate authorities. If compatibility export becomes necessary, record scope expansion first. |
| MIG-15 acceptance | same-basename, MIME mismatch, source hash, ReviewScope, 17-page regression | Each contract is testable against existing primitives; no plan defect identified. | Proceed. |
| MIG-15 visual-case input | `prepare_case_visual_sources` returns `tuple[ImmutableAttachment, ...]`; no existing public `VisualCase` value type exists, while the required boundary names a `visual_case`. | Changing the existing tuple return would break callers and conflates the established attachment intake API with the new Matter lineage boundary. | Ruling: add an immutable `VisualCase` value object plus explicit `visual_case_from_attachments(...)` constructor in `case_visual.py`; retain `prepare_case_visual_sources` unchanged. `bind_visual_case_to_matter` accepts only this exact, validated case object. `SCOPE_EXPANSION_REQUIRED = NO` because both additions are within required `case_visual.py`. |
| MIG-15 candidate case lineage | `DrawingCandidate` currently carries only `source_sha256`; two different visual cases can legally preserve identical source bytes, so source hash alone cannot distinguish a candidate from the other case. | The required fail-closed “another visual case or source hash” rule is unimplementable for identical bytes without immutable candidate `case_id` provenance. | `SCOPE_EXPANSION_REQUIRED = YES`. Add optional-but-validated `case_id` provenance to `DrawingCandidate` canonical documents and populate it in existing candidate constructors. The new Matter visual boundary rejects missing or nonmatching candidate case identity; old candidate artifacts remain readable but cannot be newly bound. This changes `contracts/drawing.py`, `parsing/drawing_candidates.py`, and their direct regressions in addition to the five required MIG-15 files. |
| MIG-15 round-1 Matter/source and producer lineage | Review found that the Matter boundary does not consult Matter source bindings, while the visual-analysis and deterministic-extractor candidate producers omit `case_id`; the active formal request binder also checks only source SHA. | A Matter/source binding must be explicit without introducing a second mutable persistence authority. Existing Matter source bindings already provide immutable source hashes, so require a complete attachment-id → existing Matter source-binding-id mapping at the new boundary and retain that exact mapping in the returned immutable context. | `SCOPE_EXPANSION_REQUIRED = YES` (continuation). Modify `review_matter/visual_binding.py`, `case_visual.py`, `drawing_review/visual_submission.py`, and `parsing/drawing_extractors.py`; add direct unit/integration regressions. Do not add a new Matter table/event because the validated expected-binding input is the accepted equivalent contract and preserves append-only Matter history. Existing candidate artifacts remain readable but fail closed in new visual and active request lineage when their `case_id` is absent. |
| MIG-15 broad-review exact attachment lineage and canonical visual identity | A candidate with only case/source/page cannot distinguish two attachments in the same case containing identical bytes, and `VisualCase` currently trusts caller-provided case/attachment IDs. | Exact candidate lineage requires an immutable `attachment_id`; downstream projection must use `(case_id, attachment_id, source_sha256, page)`. The public case boundary must derive each attachment ID and case ID from the established canonical descriptor before accepting it. | `SCOPE_EXPANSION_REQUIRED = YES` (continuation). Add optional-but-validated `attachment_id` provenance to `DrawingCandidate`, populate it in all candidate producers, require exact attachment selection in Matter/active request binders, and update case visual projection plus direct regressions. Legacy candidate documents remain readable but new Matter/active visual binding fails closed when attachment identity is absent. No new authority store or alternate identifier scheme is introduced. |

## Status

- Implementer: DONE — `9f6cf3a9f238ba1fad1f95002f5c378867e793e0`, then fix `12f8543a4d64d9974497c667c28a9867b82d1d39`.
- Task review: CHANGES_REQUESTED — P1 Matter/source lineage, P1 producer/request `case_id`, P2 missing active-path regressions.
- Task 1: fix round 1 (3 addressed, 0 open; commits `9f6cf3a`..`12f8543`; scoped re-review PASS; independent rerun partially blocked by known Windows pytest temp-directory host issue).
- Task 1: complete (commits `9f6cf3a`..`12f8543`, review clean).
- Broad review: CHANGES_REQUESTED — sol-high found P1 same-SHA distinct-attachment lineage collapse and P2 noncanonical VisualCase identity acceptance.
- Broad-review fix wave: DONE — exact candidate attachment lineage, canonical visual identity revalidation, and same-SHA projection routing fixed together; focused `49 passed`, adjacent `493 passed, 1 warning`, and targeted Ruff PASS. Commit recorded with this wave.
- Push/PR/merge: PENDING
