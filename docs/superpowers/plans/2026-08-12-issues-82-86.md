# Issues 82-86 Sequential Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve GitHub issues #82 through #86 in order while preserving offline, evidence-first, deterministic runtime contracts.

**Architecture:** First make both runtime bundle builders copy the canonical and legacy namespaces, then make Web SQLite validation fail closed. Next close DNS/socket guard bypasses, carry PDF page geometry from parser through SQLite and browser projection, and finally publish only fully built release stages atomically.

**Tech Stack:** Python 3.11+, pathlib, sqlite3, socket, tempfile, os.replace, zipfile, pytest, Ruff, mypy.

---

## Execution Rules

- Treat each GitHub issue body as the detailed acceptance contract.
- Use a failing regression test before each production behavior change.
- Preserve all existing untracked workspace artifacts.
- Keep source PDFs and parser artifacts immutable.
- Run focused tests after every issue and the repository gates before completion.

## Issue Chunks

### Chunk 1: #82 and #83 runtime packaging

- [ ] Add a shared runtime package selector returning `evidence_review` and `ansim_review`.
- [ ] Copy both packages into Codex and Web bundles.
- [ ] Execute both module entrypoints from extracted bundles.
- [ ] Replace fake SQLite fixtures with valid SQLite fixtures.
- [ ] Make Web self-test use read-only SQLite integrity and foreign-key checks.

### Chunk 2: #84 offline network guard

- [ ] Add resolver and connected-socket regression tests.
- [ ] Centralize guarded socket/resolver patch targets and preserve context restoration.
- [ ] Document application-level guard versus OS-level isolation.

### Chunk 3: #85 PDF geometry

- [ ] Extend parser and evidence page geometry with origin, rotation, and box kind.
- [ ] Canonicalize OpenDataLoader PDF bboxes into CropBox-local coordinates.
- [ ] Store metadata in schema v3 and provide a copy-on-write v2-to-v3 migration.
- [ ] Preserve rotation for display projection without changing canonical bboxes.

### Chunk 4: #86 release publication

- [ ] Move all release writes into a same-filesystem staging directory.
- [ ] Validate the completed stage and publish it atomically without overwriting an existing output.
- [ ] Add failure, retry, collision, and cleanup regression tests.

### Final validation

- [ ] Run documentation validation, pytest, Ruff, mypy, and compileall from the cleanest safe checkout available.
- [ ] Record exact HEAD, command exit codes, artifact hashes, and remaining human review requirements.
