# Example assets

`grist-desktop-qa.example.json` is retained solely as a deterministic, incomplete
fixture for the read-only legacy Grist compatibility parser tests. It is not a
runtime default, acceptance result, or user/customer workspace. New Evidence
Review workflows use the canonical source-batch and formal-review paths.