# Evidence Review System Skills

Active repo-local Codex skills now live under [`.agents/skills/`](../.agents/skills/README.md).

This legacy path is retained only as a compatibility pointer for existing repository links. No active `SKILL.md` files live under `skills/`; Codex should discover the repository-local skills from `.agents/skills/` when this repository is opened as the workspace.
